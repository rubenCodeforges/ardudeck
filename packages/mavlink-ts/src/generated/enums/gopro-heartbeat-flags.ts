export enum GoproHeartbeatFlags {
  /** GoPro is currently recording. */
  GOPRO_FLAG_RECORDING = 1,
}

/** @deprecated Use GoproHeartbeatFlags instead */
export const GOPRO_HEARTBEAT_FLAGS = GoproHeartbeatFlags;